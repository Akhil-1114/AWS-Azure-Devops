ssh -i ec2_key.pem ubuntu@<public_ip>
ssh -i ec2_key.pem ubuntu@15.207.54.172
sudo su - --- privilege access
sudo apt update -y --- update
sudo apt upgrade -y --- upgrade
apt search openjdk | grep jre --- search java versions
sudo apt install openjdk-17-jre -y --- to install OpenJDK 17 Java Runtime Environment.
curl -fsSL https://pkg.jenkins.io/debian-stable/jenkins.io-2023.key | sudo tee \
  /usr/share/keyrings/jenkins-keyring.asc > /dev/null --- "Download the official Jenkins GPG key securely and save it to the system keyring, so that APT can trust Jenkins packages in future installations."
echo deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] \
  https://pkg.jenkins.io/debian-stable binary/ | sudo tee \
  /etc/apt/sources.list.d/jenkins.list > /dev/null --- "Create a new APT source file that tells your system where to find and trust Jenkins packages, using the GPG key we saved earlier."
sudo apt-get update -y 
sudo apt-get install jenkins -y --- install jenkins
